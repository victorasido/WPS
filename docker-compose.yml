version: '3.8'

services:
  bot:
    build: .
    container_name: wordsigner_bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - DATA_DIR=/app/data
